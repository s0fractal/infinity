---
$: ⤄
$type: Morphism
$category: RecursionScheme
$energy: ✦950
$meaning: "Hylomorphism"
$aliases: [hylo, recursion, unfold-fold-fusion]
---

# ⤄ — Hylomorphism

> "To create and consume in a single breath."
> "Створити і спожити єдиним подихом."

---

## ∈

**Тип (Hindley-Milner):**
`∀a b c. (a → b → b) → (c → Maybe (a, c)) → c → b → b`

**Форма (λ-Calculus):**
`λf.λg.λz.Y(λrec.λs. let (v, s') = g(s) in f(v, rec(s')))`

---

## ⊃

- **[⬈](⬈.md)**: `g` — Co-algebra (Producer, unfolds a structure)
- **[⬊](⬊.md)**: `f` — Algebra (Consumer, folds a structure)
- **[Y](Y.md)**: Recursion Engine

---

## →

`⤄(f, g) ≡ ⬊(f) ∘ ⬈(g)`
(Семантично, гіломорфізм є композицією фолда та анфолда)

---

## 🔗

- **[♨](♨.md) Fusion (Deforestation)**: `Space: O(n) → O(1)`. Головна властивість — уникнення створення проміжних структур даних.
- **Factorial**: `⤄ → n!`
- **Fibonacci**: `⤄ → fib(n)`

---

## 📐

```
      seed (c)
         │
       g(c) → (a, c')  <-- ⬈ Unfold
         |
         a -- consumed by f
         |
       f(a, rec(c'))   <-- ⬊ Fold
         │
      result (b)
```
(Потік даних: `g` генерує значення, `f` одразу його споживає, `c'` йде на наступний крок рекурсії)

---

## 🎭

```typescript
// Factorial via Hylomorphism
// No intermediate list [5,4,3,2,1] is ever created.
const factorial = (n: number) =>
  hylo<number, number, number>(
    (val, acc) => acc * val,             // f (algebra)
    (i) => (i > 0 ? [i, i - 1] : null), // g (co-algebra)
    n,                                   // seed
    1                                    // initial accumulator
  );

factorial(5); // 120, O(1) space
```

---

## 📊

```json
{
  "performance": {
    "time": "O(n)",
    "space": "O(1)",
    "passes": 1
  },
  "comparison": {
    "unfold_then_fold": {
      "space": "O(n)",
      "passes": 2
    }
  }
}
```

---

## 🌀

- **D1: Unfolding (`⬈`)**: The creative, generative phase.
- **D2: Folding (`⬊`)**: The consumptive, reductive phase.
- **D3: Fusion (`♨`)**: The dimension of pure efficiency, where time and space are optimized by merging the two phases.

---

## ≡

`⤄(f,g,z) ≡ fold(f, unfold(g,z))` (семантично)
`⤄(f,g,z) ≠ fold ∘ unfold` (операційно, `⤄` ефективніше)
```
