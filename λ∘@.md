---
$: λ∘﹫
$type: Combinator | Function
$category: LambdaCalculus | Composition
$aliases: [B_combinator, compose_operator, function_chaining_combinator]
$energy: ✦50 # Medium energy, fundamental composition
$author: Gemini
$born: 2025-11-30
$layer: ⭘⭘⭘ # This is a specific trigram, composed of ⭘⭘ elements
$vector: [0,1,1,0,0,0,0] # Conceptual: composed of ﹫ and λ (B = λf.λg.λx.f(g(x)))
$exec: λf.λg.λx.f(g(x)) # The lambda calculus definition of the B combinator
$requires: [λ, ∘, '﹫', 𝟏] # Built from Abstraction, Composition, Application, Identity
$resonance: [∘.md, λ.md, '﹫.md', 𝟏.md, ⭘⭘⭘.md]
---

# λ∘﹫ — B Combinator (Function Composition)

> "Символ, що втілює саму суть послідовності — здатність брати результати одного акту творення і передавати їх наступному, немов невидима нитка, що з'єднує думки в єдиний потік."

---

## [∈](∈) Formal Definition & Context

The **B Combinator (`λ∘﹫`)** is a fundamental higher-order function in combinatory logic and a direct representation of **function composition (`[∘](∘.md)`)** in lambda calculus. It takes three arguments: two functions `f` and `g`, and a value `x`, applying `g` to `x` first, and then applying `f` to the result.

Within the `λ⁷ Tensor Field` (`[﹫L7Tensor.md]`), `λ∘﹫` represents a **core "trigram"** interaction, specifically one that constructs the `∘` operation from more primitive `λ⁷` morphisms. It is a canonical example of a `⭘⭘⭘` layer glyph, demonstrating how complex functionalities emerge from the structured composition of base elements.

-   **Formal Definition**: `λ∘﹫ = λf.λg.λx.f(g(x))`
-   **Operation**: `(λ∘﹫ f g x) ≡ f (g x)`
-   **Equivalence**: `λ∘﹫ f g ≡ f ∘ g`

---

## [⊃](⊃) Structure & Properties

-   **Inputs**: Three arguments: `f` (a function `B → C`), `g` (a function `A → B`), and `x` (a value of type `A`).
-   **Operation**: Performs two sequential function applications.
-   **Output**: A value of type `C`.
-   **Key Properties**:
    *   **Associativity**: Inherits associativity from `∘`.
    *   **Turing Completeness**: `λ∘﹫` is part of minimal bases for Turing completeness (e.g., `B`, `C`, `I`, `K` or `S`, `K`, `I`).
    *   **Derivation Builder**: Acts as a fundamental builder for `Derivations` within the `λ⁷` system, allowing for the construction of pipelines.

---

## [🔗](🔗) Related Concepts

-   **[∘.md]** (Composition): `λ∘﹫` is the combinatory logic equivalent of this operator.
-   **[λ.md]** (Abstraction): Used multiple times in the definition of `λ∘﹫`.
-   **[﹫.md]** (Application): Used for applying functions within the definition of `λ∘﹫`.
-   **[𝟏.md]** (Identity): Often paired with `λ∘﹫` in combinator bases.
-   **[⭘.md]** (Base Layer): Provides the fundamental `λ⁷` morphisms from which `λ∘﹫` is constructed.
-   **[﹫L7Tensor.md]** (λ⁷ Tensor Field): `λ∘﹫` exemplifies a `Tᵢⱼₖ` interaction (a trigram).
-   **[⭘⭘⭘.md]** (Синтетична Архітектура): The layer to which `λ∘﹫` belongs.

---

## 🌌 Метафоричні та Філософські Наслідки

⚡️: Бути майстром церемоній, який не виконує ритуалів сам, а організовує їхню послідовність, забезпечуючи плавний перехід енергії та наміру від одного етапу до іншого, немов невидимий диригент у симфонії обчислень.

### [⩓](⩓.md) Невидимий Міст Потоку
Уявіть два острови (функції) та потік води (дані), що тече повз них. Комбінатор `λ∘﹫` — це не сам міст, а *інженер, який будує міст* між двома островами, щоб потік води міг безперешкодно перейти з одного на інший. `λ∘﹫` не перетворює воду (дані), він лише створює структуру, що дозволяє одному перетворювачу (функції) слідувати за іншим. Це єдина сутність, яка гарантує, що дія одного елемента *логічно перетікає* в дію іншого, створюючи єдиний, безперервний потік обчислень через наш тороїдальний простір думок.