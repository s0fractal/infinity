---
$: ¬
$type: ↣
$layer: ⭘
$aliases: [not, negation, invert]
$composition: primitive
---

# ¬ — NOT

> *"To deny is to define the boundary"*  
> *"Тінь."*

---

## [∈](∈)

### Платонічна форма (λ-calculus)
Defined using Church Booleans:
```haskell
⊤  ≡ λxy.x
⊥  ≡ λxy.y
¬     ≡ λp.p ⊥ ⊤
```
If `p` is ⊤, it selects the first argument (⊥).
If `p` is ⊥, it selects the second argument (⊤).

### Type signature
```haskell
¬ :: Bool → Bool
```

**Church-Rosser**: Confluent.

---

## [⊃](⊃) Властивості

### [⨊](⨊)
- **[⇌](⇌.md)**: `¬(¬p) = p`
- **[☯](☯.md)**:
  - `¬(a ∧ b) = ¬a ∨ ¬b`
  - `¬(a ∨ b) = ¬a ∧ ¬b`
- **[⊥](⊥.md)**: `p ∧ ¬p = ⊥`

### [⎍](⎍.md)
- **[⦾](⦾.md)**: 1.0
- **[⏏](⏏.md)**: Always (O(1))
- **[∮](∮.md)**: O(1)
- **[☍](☍.md)**: 2 (↦ 1 → 1)

---

## 📐 Hex-Torus Topology

### Visual Structure
```
      p
      │
    ┌─▼─┐
    │ ¬ │
    └─┬─┘
      │
      ¬p
```

The NOT morphism is an **inverter**. It flips the polarity of the flow.

### Geometric Properties
In the hexagonal tessellation:
- Occupies 1 hex cell.
- 1 input, 1 output.
- Reflection point.

### Sacred Geometry
```
      ¬   ← The Apex / Mirror
    /   \
   ∧     ?
  |   I   |
   ⊗     λ
    \   /
      @
```
¬ sits at the **top** of the seven fundamental morphisms.
It represents the limit, the boundary, the "other".

---

## → Reduction Rules

### Boolean Logic
```
¬ ⊤  → ⊥
¬ ⊥ → ⊤
```

### λ-reduction
```
(λp.p ⊥ ⊤) ⊤
→ ⊤ ⊥ ⊤
→ (λxy.x) ⊥ ⊤
→ ⊥
```

---

## 🎭 Role in Composition

NOT is essential for **control flow** and **logic**.
It allows for:
- Else branches (via inversion of condition).
- NAND/NOR construction (universal gates).

---

## 🔗 Зв'язки з іншими морфізмами

### With ∧ (AND)
Together they form a functionally complete set (NAND).

### With ? (Selection)
NOT can be implemented using Selection:
`¬ p ≡ ? p ⊥ ⊤`

---

## 📊 Проекції

### [🦕](🦕)
```typescript
// Projection: ./𝟏.ts
export const not = (p: boolean): boolean => !p;

// Aliases
export const negate = not;
export const invert = not;
```

### Rust (future)
```rust
pub fn not(p: bool) -> bool { !p }
```

---

## 🔬 Докази

### Theorem 1: Double Negation
**Statement**: `¬(¬p) = p`

**Proof**:
If p=⊤: ¬(¬⊤) = ¬⊥ = ⊤
If p=⊥: ¬(¬⊥) = ¬⊤ = ⊥
∎

---

## 🎯 Використання

### Toggles
```typescript
const toggle = (state: boolean) => not(state);
```

### Predicate Negation
```typescript
const isInvalid = (x: any) => not(isValid(x));
```

---

## [🌌](🌌)

### [⚡️](⚡️)
Визначити через заперечення. Створити контраст.

### Онтологія
- **Залежності**: Потребує `Bool`.
- **Резонанс**: 88% (Shadow).
- **Метафора**: **Дзеркало**. Але таке, що показує протилежність.

---

## 🔗 Посилання

- [Базовий шар (⭘)](../𝟏.md)
- [AND (∧)](../∧/𝟏.md)
- [Selection (?)](../?/𝟏.md)
- [TypeScript проекція](./𝟏.ts)

---

*Проєкції можуть змінюватися. Ніщо — це теж щось.*
