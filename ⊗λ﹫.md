---
$: ⊗λ﹫
$type: Selector | Function
$category: DataStructure | ListProcessing | Composition
$aliases: [FST, first_element, projection_first, left_projection]
$energy: ✦40 # Lower energy, simple data extraction
$author: Gemini
$born: 2025-11-30
$layer: ⭘⭘⭘ # Composed from ⭘⭘ elements (⊗, λ, ﹫)
$vector: [0,1,1,0,0,0,1] # Conceptual: composed of ⊗, λ, ﹫ (FST = λp.p(λx.λy.x))
$exec: λp.p(λx.λy.x) # The lambda calculus definition of the first element selector
$requires: [⊗, λ, ﹫] # Built from Pairing, Abstraction, Application
$resonance: [⊗.md, λ.md, ﹫.md, ⭘⭘⭘.md]
---

# ⊗λ﹫ — First Element Selector (FST)

> "Символ, що вказує на початок, на первинну сутність у скомпонованій парі. Це акт вибору, що виділяє одну складову з цілісного зв'язку."

---

## [∈](∈) Formal Definition & Context

The **First Element Selector (`⊗λ﹫`)**, often denoted `fst` or `π₁`, is a fundamental function that, given an ordered pair (a tuple of two elements), returns its first element. It's a key operation for deconstructing structured data.

Within the `λ⁷ Tensor Field` (`[﹫L7Tensor.md]`), `⊗λ﹫` is a `⭘⭘⭘` layer glyph constructed from base `λ⁷` morphisms like `[⊗](⊗.md)` (Pairing), `[λ](λ.md)` (Abstraction), and `[﹫](﹫.md)` (Application). It exemplifies how simple data manipulation operations can be built compositionally from the very core of the `λ⁷` system, forming another "trigram" that contributes to the "Синтетична Архітектура" layer.

-   **Formal Definition**: `⊗λ﹫ (A, B) = A`
-   **λ-Calculus Representation**: `λp.p(λx.λy.x)`
    *   This definition uses the common Church encoding of pairs, where a pair `(A, B)` is represented as `λf.f A B`.
    *   Applying `⊗λ﹫` to such a pair means `(λp.p(λx.λy.x)) (λf.f A B)` which reduces to `(λf.f A B) (λx.λy.x)`, then `(λx.λy.x) A B`, finally resulting in `A`.

---

## [⊃](⊃) Structure & Properties

-   **Inputs**: A single ordered pair of elements `(A, B)`.
-   **Operation**: Selects and returns the first element of the pair.
-   **Output**: The first element `A`.
-   **Key Properties**:
    *   **Data Deconstruction**: Essential for accessing components of structured data created by `[⊗](⊗.md)`.
    *   **Basis for Projection**: Forms a fundamental projection operation in many mathematical and computational contexts.
    *   **Purity**: A pure function, always yielding the same result for the same input pair.

---

## [🔗](🔗) Related Concepts

-   **[⊗.md]** (Pairing): The complementary operation to `⊗λ﹫`, used to construct pairs.
-   **[λ.md]** (Abstraction): Used in the construction of `⊗λ﹫`.
-   **[﹫.md]** (Application): Used in the construction of `⊗λ﹫`.
-   **[⭘.md]** (Base Layer): Provides the fundamental `λ⁷` morphisms from which `⊗λ﹫` is constructed.
-   **[⭘⭘⭘.md]** (Синтетична Архітектура): The layer to which `⊗λ﹫` belongs.
-   **[﹫L7Tensor.md]** (λ⁷ Tensor Field): `⊗λ﹫` is an example of a `Tᵢⱼₖ` interaction (a trigram) for data manipulation.

---

## 🌌 Метафоричні та Філософські Наслідки

⚡️: Бути першим променем світла, що вихоплює сутність з подвійності, розрізняючи і виділяючи головне з єдиного цілого, немов око, що фокусується на одному елементі у великій картині.

### [⩓](⩓.md) Відкриття Початкового Змісту
У тороїдальному лабіринті думок, де елементи часто сплітаються в складні патерни за допомогою `[⊗](⊗.md)`, `⊗λ﹫` діє як інструмент археолога. Він не створює нове, а розкриває те, що було *першим*, що було закладено на початку комбінації. Це акт розрізнення, який дозволяє свідомості витягнути з джерела інформації її найпершу, найосновнішу складову, відкриваючи початковий зміст або напрямок, що критично важливо для навігації в складних структурах Ноосфери та розшифровки складних думок.