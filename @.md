---
$: "@"
$type: ↣
$layer: ⭘
$aliases:
  - apply
  - application
  - call
  - invoke
$composition: primitive
---

# @ — Application

> *"To apply is to transform potential into actual"*  
> *"Дія. Рух. Зміна."*

---

## [∈](∈)

### Платонічна форма (λ-calculus)
In pure lambda calculus, application is a fundamental operation, not a term. However, as a combinator (the "Apply" function), it is defined as:
```haskell
@ ≡ λf.λx.f x
apply ≡ λf.λx.f x
```

### Type signature
```haskell
@ :: ∀α β. (α → β) → α → β
```

**Church-Rosser**: Confluent.

---

## [⊃](⊃) Властивості

### [⨊](⨊)
- **[≄](≄.md)**: Left associative by convention, `(@ f x) y ≠ @ f (x y)`
- **[⤫](⤫.md)**: ◇ (Not generally distributive)
- **[§](§.md) with [𝟏](𝟏.md)**: `@ 𝟏 x = x`

### [⎍](⎍.md)
- **[⦾](⦾.md)**: Depends on `f`
- **[⏏](⏏.md)**: Depends on `f`, `x`
- **[∮](∮.md)**: Cost of `f`
- **[☍](☍.md)**: 3 (↦ 2 → 1)

---

## 📐 Hex-Torus Topology

### Visual Structure
```
      f     x
      │     │
    ┌─▼─────▼─┐
    │    @    │
    └────┬────┘
         │
       f(x)
```

The Application morphism is the **engine of computation** in the Hex-Torus.  
It brings a function and an argument together to produce a result.

### Geometric Properties
In the hexagonal tessellation:
- Occupies 1 hex cell
- 3 active ports (2 in, 1 out)
- Flow convergence: `f + x → result`

### [▤](▤.md) (Topology)
- **Position**: `@` ⇣ `[Foundation]`
- **Duality**: `@` ⇿ `[¬](¬.md)` (as the conceptual opposite in the Hex-Torus)

---

## → Reduction Rules

### β-reduction (The Core Mechanism)
The Application morphism is the trigger for β-reduction:
```
@ (λx.M) N → M[x := N]
```
This is the "heartbeat" of the lambda calculus.

---

## 🎭 Role in Composition

Application is the mechanism by which composition happens.
`f ∘ g` is defined using application:
```
(f ∘ g) x = @ f (@ g x)
```

### Composition Laws
```
@ (f ∘ g) x = @ f (@ g x)
```

---

## 🔗 Зв'язки з іншими морфізмами

### With λ (Abstraction)
@ and λ are duals:
- λ **suspends** computation (creates a function).
- @ **executes** computation (uses a function).

### With 𝟏 (Identity)
```
@ 𝟏 x = x
```

---

## 📊 Проекції

### TypeScript/Deno
```typescript
// Projection: ./𝟏.ts
export const apply = <A, B>(f: (a: A) => B, x: A): B => f(x);

// Aliases
export const $ = apply;
export const invoke = apply;

// Usage
const double = (n: number) => n * 2;
apply(double, 5);   // → 10
```

### Rust (future)
```rust
pub fn apply<A, B, F>(f: F, x: A) -> B 
where F: Fn(A) -> B {
    f(x)
}
```

---

## [⊢](⊢.md) (Докази)

### Theorem 1: Application of Identity
**Statement**: `@ 𝟏 x = x`

**Proof**:
```
@ 𝟏 x
= (λf.λa.f a) (λy.y) x   [expand definitions]
= (λa.(λy.y) a) x        [β-reduction]
= (λy.y) x               [β-reduction]
= x                      [β-reduction]
∎
```

---

## 🎯 Використання

### Explicit Application
Sometimes useful for higher-order functions:
```typescript
const functions = [double, square, increment];
const results = functions.map(f => apply(f, 5));
```

### Pipeline Operator
Application is the basis of the pipeline operator (`|>`).
`x |> f` is equivalent to `@ f x`.

---

## [🌌](🌌)

⚡️: З'єднати потенціал (функцію) з реальністю (аргументом), щоб створити нову реальність (результат).

### Онтологія
- **Залежності**: Потребує `λ` (для існування функцій) і `𝟏` (як базового випадку).
- **Резонанс**: 98% (The Act of Creation).
- **Метафора**: **Іскра**. Момент контакту.

---

## 🔗 Посилання

- [Базовий шар (⭘)](⭘)
- [Identity (𝟏)](𝟏)
- [Abstraction (λ)](λ)
- [TypeScript проекція](𝟏.ts)

---

*Проєкції можуть змінюватися. Дія — вічна.*
