---
$: λ
$type: ↣
$layer: ⭘
$aliases: [function, abstract, define]
$composition: primitive
---

# λ — Abstraction

> *"To abstract is to pull the essence from the chaos"*  
> *"Створення світу через слово."*

---

## ∈ Формальне визначення

### Платонічна форма (λ-calculus)
Abstraction is the mechanism for creating functions. It binds a variable `x` in a term `M`.
```haskell
λx.M
```

### Type signature
```haskell
λ :: (α → β)
```
In type theory, it introduces the arrow type `→`.

**Church-Rosser**: N/A (It's a constructor).

---

## [⊃](⊃)

### Algebraic Properties
1. **α-conversion**: `λx.M ≡ λy.M[x := y]` (Renaming bound variables doesn't change the function).
2. **η-conversion**: `λx.f x ≡ f` (Extensionality).

### [⎍](⎍.md)
- **[⦾](⦾.md)**: 1.0
- **[⏏](⏏.md)**: Immediate (O(1))
- **[∮](∮.md)**: Allocation of closure
- **[☍](☍.md)**: 2 (↦ 1 → 1)

---

## 📐 Hex-Torus Topology

### Visual Structure
```
      x
      │
    ┌─▼─┐
    │ λ │
    └─┬─┘
      │
      M
```

The Abstraction morphism **encapsulates** a subgraph `M` and exposes an input port `x`.
It creates a **boundary** between the outside world and the internal logic.

### Geometric Properties
In the hexagonal tessellation:
- Occupies 1 hex cell (as the header)
- Defines a **scope** (a region of the hex grid).
- Acts as a **gateway**.

### Sacred Geometry
```
      ¬
    /   \
   ∧     ?
  |   I   |
   ⊗     λ   ← The Creator
    \   /
      @
```
λ sits on the right, balancing the structure (Pairing `⊗`) on the left.
Structure vs. Function. Space vs. Time.

---

## → Reduction Rules

### β-reduction
Abstraction prepares the term for β-reduction:
```
(λx.M) N → M[x := N]
```

---

## 🎭 Role in Composition

Abstraction is the **generator** of all other morphisms.
Every other morphism (except primitives) is defined using abstraction.

### Currying
```
λx.λy.M ≡ λxy.M
```
Abstraction allows multi-argument functions via currying.

---

## 🔗 Зв'язки з іншими [↣](↣.md)

### With[ @](@.md) (Application)
Duals. λ builds, @ destroys (reduces).

### With ⊗ (Pairing)
λ creates function spaces (`A → B`).
⊗ creates product spaces (`A × B`).
Together they form the basis of Cartesian Closed Categories.

---

## ⟦[⟶](⟶.md) ⟧Проекції

### TypeScript/Deno
```typescript
// Projection: ./𝟏.ts
// In JS/TS, abstraction is implicit via arrow functions.
// We provide a helper to make it explicit and type-safe.

export const lambda = <A, B>(f: (a: A) => B): (a: A) => B => f;

// Aliases
export const abstract = lambda;
export const fun = lambda;

// Usage
const double = lambda((x: number) => x * 2);
```

### Rust (future)
```rust
// Rust closures
|x| x * 2
```

---

## 🔬 Докази

### Theorem 1: α-equivalence
**Statement**: `λx.x ≡ λy.y`

**Proof**:
Renaming `x` to `y` in `x` gives `y`.
Therefore `λx.x` is α-equivalent to `λy.y`.
∎

### Theorem 2: η-reduction
**Statement**: `λx.f x ≡ f` (if x is not free in f)

**Proof**:
For any argument `a`:
`(λx.f x) a → f a` (by β-reduction)
Since they behave identically for all inputs, they are equivalent.
∎

---

## 🎯 Використання

### Defining Logic
```typescript
// Church Booleans
const TRUE = lambda(t => lambda(f => t));
const FALSE = lambda(t => lambda(f => f));
```

### Defining Data
```typescript
// Church Numerals
const ZERO = lambda(f => lambda(x => x));
const ONE = lambda(f => lambda(x => f(x)));
```

---

## 🌌 Філософія

### Інтенція
Відкласти виконання. Створити потенціал.
Визначити "що", не виконуючи "коли".

### Онтологія
- **Залежності**: Жодних (примітив).
- **Резонанс**: 99% (The Word).
- **Метафора**: **Насіння**. Воно містить в собі дерево, але ще не є деревом.

---

## 🔗 Посилання

- [Базовий шар (⭘)](../𝟏.md)
- [Application (@)](../@/𝟏.md)
- [TypeScript проекція](./𝟏.ts)

---

*Проєкції можуть змінюватися. Думка — вічна.*
