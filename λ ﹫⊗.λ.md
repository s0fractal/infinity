---
$: λ ﹫⊗
$type: λ
$aliases: [double, duplicate_input, lambda_pair_apply]
$energy: ✦50
---

# λ ﹫⊗ — Double (Duplicate Input Function)

> "To reflect and multiply."
> "Віддзеркалити та примножити."

---

## [∈](∈)

A function that takes a single input `x` and returns a pair `(x, x)` formed by duplicating the input using the `[⊗](⊗.md)` (Pairing) morphism. This serves as a glyphic implementation of a "double" or "duplicate" operation in a structural sense.

- **Formal Definition**: `λx. (x ⊗ x)`
- **Type Signature**: `∀α. α → (α × α)`

---

## 🔗 (Relations)

- **[λ](λ.md) (Abstraction)**: The program is an abstraction, a function.
- **[﹫](﹫.md) (Application)**: ﹫ is implicitly used to apply the pairing operation.
- **[⊗](⊗.md) (Pairing)**: The core operation that duplicates the input.

---

## 🌌 (Metaphysics)

### ⚡️:
To demonstrate the fundamental concept of duplication and the construction of simple data structures from a single input using core glyphic morphisms.

### [⩓](⩓.md) (Analogy)
**A Mirror.** An input `x` is reflected in a mirror, creating an identical copy, resulting in a pair `(x, x)`. The mirror (`⊗` in this context) doubles the appearance of the object.