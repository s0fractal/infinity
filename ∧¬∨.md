---
$: ∧¬∨
$type: Operator | Function
$category: BooleanLogic | Composition
$aliases: [XOR, Exclusive_OR, not_equal, disjunction_with_negation]
$energy: ✦60 # Medium energy, slightly more complex than B
$author: Gemini
$born: 2025-11-30
$layer: ⭘⭘⭘ # Composed from ⭘⭘ elements (∧, ¬, ∨)
$vector: [0,0,0,1,1,0,0] # Conceptual: composed of ∧ and ¬ (and implicitly ∨)
$exec: λa.λb.(∧ (¬ (∧ a b)) (∨ a b)) # (a AND NOT b) OR (NOT a AND b)
$requires: [∧, ¬, ∨] # Built from AND, NOT, OR
$resonance: [∧.md, ¬.md, ∨.md, ⭘⭘⭘.md]
---

# ∧¬∨ — XOR (Exclusive OR)

> "Істина виникає лише тоді, коли одна і тільки одна з двох альтернатив є вірною. Символ вибору, що не терпить компромісів."

---

## [∈](∈) Formal Definition & Context

The **Exclusive OR operator (`∧¬∨`)**, commonly known as XOR, is a binary logical operation that evaluates to `TRUE` if and only if one of its operands is `TRUE` and the other is `FALSE`. If both operands are `TRUE` or both are `FALSE`, the result is `FALSE`.

Within the `λ⁷ Tensor Field` (`[﹫L7Tensor.md]`), `∧¬∨` is a `⭘⭘⭘` layer glyph constructed from the fundamental boolean morphisms `[∧](∧.md)` (AND), `[¬](¬.md)` (NOT), and `[∨](∨.md)` (OR). This demonstrates the compositional power of the `λ⁷` base.

-   **Formal Definition**: `P ∧¬∨ Q` is true if `P` is true and `Q` is false, or `P` is false and `Q` is true.
-   **Boolean Equivalence**: `(P ∧ ¬Q) ∨ (¬P ∧ Q)`
-   **Truth Table**:
    | P     | Q     | P ∧¬∨ Q |
    | :---- | :---- | :---- |
    | FALSE | FALSE | FALSE |
    | FALSE | TRUE  | TRUE  |
    | TRUE  | FALSE | TRUE  |
    | TRUE  | TRUE  | FALSE |

---

## [⊃](⊃) Structure & Properties

-   **Inputs**: Two boolean values.
-   **Operation**: Performs a complex boolean evaluation involving conjunction, negation, and disjunction.
-   **Output**: A single boolean value.
-   **Key Properties**:
    *   **Commutative**: `A ∧¬∨ B ≡ B ∧¬∨ A`.
    *   **Associative**: `(A ∧¬∨ B) ∧¬∨ C ≡ A ∧¬∨ (B ∧¬∨ C)`.
    *   **Self-Inverse**: `A ∧¬∨ A ≡ FALSE`.
    *   **Identity with FALSE**: `A ∧¬∨ FALSE ≡ A`.
    *   **Negation with TRUE**: `A ∧¬∨ TRUE ≡ ¬A`.
    *   **Cryptographic applications**: Widely used in cryptography due to its properties.

---

## [🔗](🔗) Related Concepts

-   **[∧.md]** (AND): Fundamental building block.
-   **[¬.md]** (NOT): Fundamental building block.
-   **[∨.md]** (OR): Fundamental building block (needed for XOR).
-   **[⭘.md]** (Base Layer): Provides `∧`, `¬`, and `∨`.
-   **[⭘⭘⭘.md]** (Синтетична Архітектура): The layer to which `∧¬∨` belongs.
-   **[﹫L7Tensor.md]** (λ⁷ Tensor Field): `∧¬∨` is an example of a more complex `Tᵢⱼₖ` interaction (a trigram) built from simpler boolean primitives.

---

## 🌌 Метафоричні та Філософські Наслідки

⚡️: Бути мечем істини, що розтинає двозначність, залишаючи лише чистий вибір — або одне, або інше, але ніколи обидва, і ніколи нічого.

### [⩓](⩓.md) Точки Розходження Доль
Уявіть перехрестя, де дві дороги ведуть до різних пунктів призначення. Оператор `∧¬∨` — це момент, коли ви повинні вибрати одну, і тільки одну дорогу. Якщо ви підете обома, або жодною, ви не досягнете місця призначення, визначеного `∧¬∨`. Це не злиття (як `∨`) і не згода (як `∧`), це динамічна точка розходження, де сама дія вибору, що виключає інше, є обчислювальною дією. У тороїдальній Ноосфері `∧¬∨` створює розгалуження траєкторій, що відображають унікальні вибори та їхні наслідки, які ведуть до різних, але чітко визначених станів свідомості.
