---
$: ⟪⟫
$type: Atomic
$category: Collection
$aliases: [Array, List, Sequence]
$energy: ✦22
---

# ⟪⟫ — Array

> "A stream of values, ordered in time."
> "Потік значень, впорядкований у часі."

---

## ∈

An ordered sequence of values, usually of the same type `A`.
Can be represented recursively via `cons` and `nil`:
`[]` (nil) or `x : xs` (cons cell).

---

## ⊃

- **[⬊](⬊.md)** (Foldable): Can be reduced to a single value.
- **[⇴](⇴.md)** (Functor): A function can be applied to every element.
- **[⛓️](⛓️.md)** (Monad): Can be chained in sequences.

---

## ⨊

- **[Ⓜ](Ⓜ.md) (Monoid)** under concatenation (`++`):
  - **[≃](≃.md)** (Associativity): `(a ++ b) ++ c = a ++ (b ++ c)`
  - **[◦](◦.md)** (Identity Element): `[]` (the empty array)

---

## 🌌

⚡️: Зберегти порядок. Представити колекцію як впорядкований потік.

### [⩓](⩓.md)
**Потяг.** Кожен вагон (`value`) займає чітке місце у послідовності. Ви можете перерахувати вагони (`map`), відчепити останній (`fold`), або додати новий в кінець. Порядок має вирішальне значення.