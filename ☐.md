---
$: ☐
$type: Type
$category: ControlFlow
$aliases: [optional, maybe, option_type, nullable]
$energy: ✦180
---

# ☐ — Optional (Maybe Type)

> "The possibility of absence."
> "Можливість відсутності."

---

## [∈](∈)

A type constructor that represents the explicit possibility of a value being present (`[⊚](⊚.md)`) or absent (`[∅](∅.md)`), without signifying a computational error or failure. It provides a way to handle situations where a function may or may not return a meaningful result.

---

## 🔗 (Relations)

- **[↯](↯.md) (Failure)**: `☐` explicitly distinguishes the absence of a value (e.g., "no result found") from a computational `↯` (e.g., "an error occurred during calculation").
- **[◇](◇.md) (Unit)**: `◇` is a specific value (the unit value `()`), whereas `☐` is a *type* that can contain a value or nothing.
- **[⊚](⊚.md) (Some)**: One of the two constructors for `☐`, indicating the presence of a value.
- **[∅](∅.md) (None)**: The other constructor for `☐`, indicating the absence of a value.

---

## 🌌 (Metaphysics)

### ⚡️:
To explicitly model and manage the uncertainty of value presence. To enable robust handling of computations that might legitimately not produce a result without resorting to side effects or error mechanisms.

### [⩓](⩓.md) (Analogy)
**A Parking Spot.** A parking spot (`☐`) can either contain a car (`⊚`) or be empty (`∅`). The emptiness does not mean the parking spot is broken (`↯`); it simply means no car is currently occupying it.
