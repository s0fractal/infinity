---
$: ⬊
$type: Morphism
$category: Catamorphism
$aliases: [fold, reduce, cata, crush]
$energy: ✦850
---

# ⬊ — Fold (Catamorphism)

> "The universal consumer of structures."
> "Універсальний споживач структур."

---

## ∈

**Визначення (для списків):**
`foldr f z [] = z`
`foldr f z (x:xs) = f x (foldr f z xs)`

**Тип:**
`∀a,b. (a → b → b) → b → List a → b`

---

## ⊃

- **[Y](Y.md)**: (Неявно використовує рекурсію)
- **f**: Algebra (функція, що "згортає")
- **z**: Accumulator (початкове значення)

---

## ⨊

- **[𝕌](𝕌.md) (Universal Property)**: `h = ⬊ f z ⇔ (h [] = z ∧ h (x:xs) = f x (h xs))`
- **[♨](♨.md) (Fusion Law)**: `h ∘ ⬊ f z = ⬊ g (h z)`

---

## →

`⬊ f z [x, y] → f(x, f(y, z))`

---

## 🔗

- **Dual**: є дуальним до `[⬈](⬈.md)` (Unfold).
- **Universal**: Будь-яку функцію над списком (`map`, `filter`, `length`...) можна виразити через `⬊`.
- **Foundation**: Основа для всіх операцій агрегації (sum, product, max).

---

## 📐

```
  [x, y, z]
   │  │  │
   ▼  ▼  ▼
f(x, f(y, f(z, init)))
         │
         ▼
       result
```

---

## 🎭

```typescript
// Сума елементів списку
const sum = (list: number[]) =>
  foldl((acc, x) => acc + x, 0, list);

sum([1, 2, 3, 4]); // → 10

// Map через fold
const map = <A, B>(f: (a: A) => B, list: A[]): B[] =>
  foldr((x, acc) => [f(x), ...acc], [], list);

map(x => x * 2, [1, 2, 3]); // → [2, 4, 6]
```

---

## 📊

```json
{
  "purity": "Depends on f",
  "termination": "Guaranteed for finite structures",
  "universality": "Can express any list function"
}
```

---

## 🌀

- **D1: Consumption**: Вимір споживання, руйнування структури.
- **D2: Aggregation**: Вимір зведення множини до одного.

---

## ≡

- `reduce`, `inject` (в багатьох мовах програмування).
- **Catamorphism** (в теорії категорій).
```
