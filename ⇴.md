---
$: ⇴
$type: Morphism
$category: Functor
$aliases: [map, fmap, lift, transform]
$energy: ✦900
---

# ⇴ — Map (Functor)

> "To transform the contents, not the container."
> "Трансформувати вміст, а не контейнер."

---

## ∈

**Визначення (для списків):**
`map f [] = []`
`map f (x:xs) = f(x) : map f xs`

**Тип:**
`∀a,b,F. Functor F => (a → b) → F a → F b`

---

## ⊃

- **[⬊](⬊.md)**: Може бути визначений через `fold`.
- `f`: Функція-трансформація.
- `F a`: Функтор (контейнер) зі значеннями.

---

## ⨊ (Functor Laws)

- **[◦](◦.md) Identity**: `⇴ id = id`
- **[∘](∘.md) Composition**: `⇴ (f ∘ g) = (⇴ f) ∘ (⇴ g)`

---

## 🔗

- **Functor**: `⇴` є визначальною операцією для всіх функторів.
- **[♨](♨.md) Fusion**: `map(f) ∘ map(g) ≡ map(f ∘ g)` (оптимізація).

---

## 📐

```
[a, b, c] -- F<a>
 │  │  │
 f  f  f   -- (a→b)
 │  │  │
 ▼  ▼  ▼
[f(a), f(b), f(c)] -- F<b>
(Структура збережена)
```

---

## 🎭

```typescript
// Подвоєння кожного елемента в масиві
const double = (x: number) => x * 2;
map(double, [1, 2, 3]); // → [2, 4, 6]

// Перетворення на великі літери
const toUpper = (s: string) => s.toUpperCase();
map(toUpper, ["a", "b"]); // → ["A", "B"]
```

---

## 📊

```json
{
  "purity": "Depends on f",
  "termination": "Guaranteed for finite structures",
  "structure_preservation": true,
  "parallelizable": true
}
```

---

## 🌀

- **D1: Transformation**: Вимір перетворення вмісту.
- **D2: Preservation**: Вимір збереження структури/контексту.

---

## ≡

- `fmap` (в Haskell).
- `.map()` (метод масивів, `Option`, `Result` тощо).
- `transform` (в C++).
```
