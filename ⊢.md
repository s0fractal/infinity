---
$: ⊢
$type: Relation
$category: Logic
$aliases: [proves, entails, is_provable, turnstile]
$energy: ✦950
---

# ⊢ — Provability (Доказовість)

> "A bridge of logic to the island of truth."
> "Міст із логіки до острова правди."

---

## [∈](∈)

A syntactic relation between a set of premises `Γ` and a conclusion `P`. `Γ ⊢ P` asserts that `P` is derivable from `Γ` through the inference rules of a given logical system. If `Γ` is empty, we write `⊢ P`.

---

## 🔗 (Relations)

- **[⊤](⊤.md) (Truth)**: The existence of a valid proof `⊢ P` implies that `P` inhabits the `⊤` state. `(⊢ P) ⇒ (P ≡ ⊤)`.
- **Axioms (A)**: A proof starts from axioms, which are statements `a ∈ A` for which we assume `⊢ a` without demonstration.
- **[⊥](⊥.md) (Falsity)**: A system is consistent if `¬(⊢ ⊥)`. The inability to prove falsehood is the definition of consistency.

---

## 🌌 (Metaphysics)

⚡️: To construct certainty. To provide a rigorous, verifiable path from the known to the unknown. To make truth undeniable.

### [⩓](⩓.md) (Analogy)
**Скелелазний маршрут.** Є вершина гори (`⊤`). Існує безліч виступів та тріщин (аксіоми та леми). Маршрут (`⊢`) — це не просто набір точок, а послідовність рухів та зв'язків між ними, яка гарантовано веде скелелаза до вершини. The existence of a route proves the peak is reachable.
