---
$: ⇈
$type: Property
$energy: ✦75
$meaning: "Bifunctoriality"
$aliases: [bifunctor, parallel-map]
---

# ⇈ — Bifunctoriality

> "To act on both parts as one."
> "Діяти на обидві частини як на єдине ціле."

---

## ∈ Формальне визначення

`map (f, g) (pair x y) = pair (f x) (g y)`

---

## 🌌 Філософія

### Інтенція
Визначити відображення, що зберігає структуру і діє на обидві компоненти біфунктора (наприклад, добутку типів) одночасно.

### Метафора
**Двоє людей, що разом перефарбовують двоколірну машину.** Один маляр (`f`) фарбує свою половину (`x`), інший (`g`) — свою (`y`). Вони діють узгоджено, і в результаті виходить одна цілісна перефарбована машина.
