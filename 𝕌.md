---
$: 𝕌
$type: MetaProperty
$category: CategoryTheory
$aliases: [universal-property, universal-mapping-property]
$energy: ✦990
---

# 𝕌 — Universal Property

> "To be defined not by what you are, but by how you uniquely relate to everything else."
> "Бути визначеним не тим, що ти є, а тим, як ти унікально пов'язаний з усім іншим."

---

## ∈

**Визначення:**
Універсальна властивість визначає об'єкт, стверджуючи існування **унікального** морфізму (стрілки) до нього або від нього для будь-якого іншого об'єкта в категорії.

**Приклад (для [⬊](⬊.md) Fold):**
`h = ⬊ f z` тоді й лише тоді, коли `h` є **єдиним** морфізмом, що задовольняє умови:
1. `h [] = z`
2. `h (x:xs) = f x (h xs)`

---

## 🔗

- **[⬊](⬊.md) (Fold/Catamorphism)**: `𝕌` гарантує, що `fold` є єдиним канонічним "споживачем" структури.
- **[⬈](⬈.md) (Unfold/Anamorphism)**: `𝕌` гарантує, що `unfold` є єдиним канонічним "творцем" структури.

---

## [🌌](🌌)

### [⚡️](⚡️)
Визначити концепт не тим, що він *є*, а тим, як він *унікально взаємодіє* з усім іншим. Надати настільки точний "відбиток пальця" для структури, що він може описувати лише одну річ у всесвіті.

### [⩓](⩓.md)
**Ключ до замка.** Існує безліч предметів, але лише один унікальний ключ (`h`) ідеально підходить до замка (властивостей `fold f z`). Універсальна властивість — це твердження, що такий унікальний ключ існує, і ми можемо його знайти.
