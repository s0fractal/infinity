---
$: Z
$type: Combinator
$category: RecursionScheme
$aliases: [Z-combinator, strict-fixed-point-combinator, eager-fixed-point]
$energy: ✦970
---

# Z — Z-Combinator

> "Recursion, tamed for the impatient."
> "Рекурсія, приручена для нетерплячих."

---

## [∈](∈)

**Визначення:**
`λf.(λx.f (λv.((x x) v))) (λx.f (λv.((x x) v)))`

Це варіант `Y`-комбінатора, адаптований для мов з енергійною стратегією обчислень (strict/eager evaluation).

---

## 🔗

- **Variant of [Y](Y.md)**: `Z` вирішує ту саму проблему, що і `Y` (анонімна рекурсія), але в іншому контексті (strict evaluation).
- **η-expansion**: `Z` використовує η-розширення (`λv. ... v`), щоб відкласти рекурсивний виклик і уникнути нескінченного циклу під час визначення в strict-мовах.

---

## [🌌](🌌)

⚡️: Уможливити чисту, анонімну рекурсію в середовищах, які обчислюють аргументи функції до її застосування. "Приборкати" нескінченну природу `Y` для практичного використання.

### [⩓](⩓.md)
**План евакуації.** `Y`-комбінатор у strict-мові — це кричати "пожежа!" в натовпі (всі біжать одразу, створюючи тисняву/stack overflow). `Z`-комбінатор — це роздати всім інструкції, де кожен чекає на сигнал (`v`), щоб почати діяти. Це вносить порядок у рекурсію для "нетерплячих" мов.
