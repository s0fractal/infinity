---
$: 🔍
$type: TemplateMorphism
$category: Optics
$aliases: [lens, focus]
$energy: ✦700
---

# 🔍 — Lens

> "A functional reference to a part of a whole."
> "Функціональне посилання на частину цілого."

---

## ∈

**Визначення:**
Лінза `s -> a` — це пара з `get` та `set`:
`get: s → a`
`set: a → s → s`

---

## ⊃

- **[🛠️](🛠️.md)** (Make): Лінзи генеруються з `make`-примітивів.

---

## ⨊ (Lens Laws)

- **Get-Set**: `set l (get l s) s ≡ s`
  _(Якщо дістати значення і одразу вставити його назад — нічого не зміниться)._
- **Set-Get**: `get l (set l v s) ≡ v`
  _(Якщо вставити значення і одразу дістати його — отримаємо це саме значення)._
- **Set-Set**: `set l v₂ (set l v₁ s) ≡ set l v₂ s`
  _(Дві вставки поспіль еквівалентні одній останній вставці)._

---

## 🔗

- **Composable**: `🔍 ∘ 🔍`. Лінзи можна композиціонувати для фокусування на глибоко вкладених даних.
- **State Management**: Є основою для сучасного імутабельного менеджменту стану.

---

## 📐

```
     S (whole)
    / \
get |   | set(a)
    \ /
     A (part)
```

---

## 🎭

```typescript
const user = { address: { street: "Main St" } };

// Створюємо лінзи для доступу до полів
const addressLens = lens(prop('address'));
const streetLens = lens(prop('street'));

// Композиція лінз для глибокого доступу
const userStreetLens = compose(addressLens, streetLens);

// Читання даних через лінзу
view(userStreetLens, user); // → "Main St"

// Оновлення даних через лінзу (імутабельно)
const updatedUser = set(userStreetLens, "New St", user);
// → { address: { street: "New St" } }
```

---

## 📊

```json
{
  "purity": 1.0,
  "composability": ⊤
}
```

---

## 🌀

- **D1: Focus**: Вимір фокусування, здатність "наблизити" частину великої структури.
- **D2: Immutability**: Вимір незмінності, гарантія того, що оновлення створюють нову копію, не змінюючи оригінал.

---

## ≡

- **Optic** (в теорії категорій).
- `Lens` (в Haskell, Scala, etc).
```