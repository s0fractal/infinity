---
$: ⬈
$type: Morphism
$category: Anamorphism
$aliases: [unfold, ana, generate, corecursion]
$energy: ✦850
---

# ⬈ — Unfold (Anamorphism)

> "The universal creator of structures."
> "Універсальний творець структур."

---

## [∈](∈)

**Визначення (для списків):**
`unfold f z = case f z of Nothing → [] ; Just (x, z') → x : unfold f z'`

**Тип:**
`∀a,b. (b → Maybe (a, b)) → b → [a]`

---

## ⊃

- **[Y](Y.md)**: (Неявно використовує рекурсію/корекурсію)
- **f**: Co-algebra (функція, що "розгортає")
- **z**: Seed (початкове значення, "насінина")

---

## ⨊

- **[𝕌](𝕌.md) (Universal Property)**: `h = ⬈ g` тоді й лише тоді, коли `h` задовольняє певним рекурсивним умовам.
- **[☯](☯.md) (Duality)**: Є дуальним до `[⬊](⬊.md)`.

---

## →

`⬈ g z → []`, якщо `g(z) = Nothing`
`⬈ g z → x : ⬈ g z'`, якщо `g(z) = Just(x, z')`

---

## 🔗

- **Dual**: є дуальним до `[⬊](⬊.md)` (Fold).
- **Composition**: Композиція з `⬊` утворює `[⤄](⤄.md)` (Hylomorphism).
- **Foundation**: Основа для генераторів, ітераторів та потоків.

---

## 📐

```
       seed
         │
         ▼
g(seed) → [value, next_seed]
             │         ↘
           value     g(next_seed) → ...
```

---

## 🎭

```typescript
// Генерація числового діапазону
const range = (n: number) =>
  unfold((i) => (i < n ? [i, i + 1] : null))(0);

range(5); // → [0, 1, 2, 3, 4]

// Послідовність Фібоначчі
const fibonacci = (n: number) => unfold(
  ([a, b, count]) => count < n ? [a, [b, a + b, count + 1]] : null
)([0, 1, 0]);

fibonacci(8); // → [0, 1, 1, 2, 3, 5, 8, 13]
```

---

## 📊

```json
{
  "purity": "Depends on f",
  "termination": "Not Guaranteed"
}
```

---

## 🌀

- **D1: Creation**: Вимір творення, генерації структури з початкового стану.
- **D2: Corecursion**: Процес покрокового будування даних.

---

## ≡

- **Anamorphism** (в теорії категорій).
- **Generators / Iterators** (в багатьох мовах програмування).
```
