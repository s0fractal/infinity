---
$: ∘
$type: Operator
$category: CategoryTheory | Computation | TensorInteraction
$aliases: [compose, B-combinator, function_composition, sequential_application]
$energy: ✦1000
$author: Original_Author | Gemini_Refinement
$born: before-time | 2025-11-30
---

# ∘ — Composition

> "Суть послідовної трансформації. Фундаментальний механізм, за допомогою якого окремі обчислювальні операції об'єднуються в ланцюжок, дозволяючи виходу однієї ставати входом наступної, таким чином формуючи функції вищого порядку в тензорному полі λ⁷."

---

## [∈](∈) Formal Definition & Context

**Composition `∘`** is a binary operator `(f ∘ g)` that combines two functions `g: A → B` and `f: B → C` to produce a new function `A → C`. It represents the sequential application of `g` followed by `f`, i.e., `(f ∘ g)(x) ≡ f(g(x))`.

Within the **λ⁷ Tensor Field** (`[@L7Tensor.md]`), `∘` is a **specific tensor interaction** handled by the "Composition Unit" of the `λ⁷-CPU`. It links output trajectories of `g` directly into input trajectories of `f`, yielding a new composed "vector" (function/derivation) in the `λ⁷` space.

-   **[λ](λ.md)-Calculus**: `λf.λg.λx.f(g(x))` (B combinator).
-   **Type Signature**: `∀α,β,γ. (β → γ) → (α → β) → (α → γ)`

---

## [⊃](⊃) Structure & Properties

-   **Inputs**: Two functions, `f` and `g`, where `codomain(g) = domain(f)`. Functions can be base `λ⁷` morphisms (`[⭘](⭘.md)`) or composed functions/derivations.
-   **Operation**: Chaining of functions; a tensor operation `T` where `f` and `g` create a new entity by mapping `output(g)` to `input(f)`.
-   **Output**: A new, single function encapsulating combined behavior of `f` and `g`. This output is a valid `λ⁷` derivation, further composable.
-   **Key Properties**:
    *   **Associativity ([≃](≃.md))**: `(h ∘ g) ∘ f = h ∘ (g ∘ f)`.
    *   **Identity ([𝟏](𝟏.md))**: `f ∘ 𝟏 = f` and `𝟏 ∘ f = f`.
    *   **Non-Commutativity ([⇆](⇆.md))**: `f ∘ g ≠ g ∘ f` generally.
    *   **Foundation of Derivations**: `∘` constructs `Derivations` (complex programs) from individual `λ⁷` morphisms.

---

## [🔗](🔗) Related Concepts

-   **[⭘](⭘.md)** (Base Layer): Provides atomic `λ⁷` morphisms.
-   **[@L7Tensor.md]** (λ⁷ Tensor Field): Formal framework defining `∘`'s interaction (`Tᵢⱼₖ`). `eval` from `@L7Tensor.md` uses `∘`.
-   **[λ_universal.md]** (Universal Function): `ProgramEncoding` is `Derivation` (sequence of morphisms composed via `∘`).
-   **[I](I.md), [@](@.md), [λ](λ.md)**: `∘` can be constructed from these.
-   **[→](→.md)** (Morphism): Composition chains these transformations.
-   **[B](𝔹.md)** (B-combinator): Combinatorial equivalent.
-   **Category Theory**: `∘` is a defining operation of a category.

---

## 🌌 Metaphorical & Philosophical Implications

⚡️: Бути невидимою ниткою, що сплітає окремі акти творення в єдину, безперервну історію, де кожен крок є логічним продовженням попереднього, а ціле перевершує суму його частин.

### [⩓](⩓.md) The Recursive Chain of Causality
Уявіть серію дзеркал, розташованих одне за одним. Зображення, яке відбивається в першому, стає входом для другого, потім для третього, і так далі. Кожне дзеркало є функцією, а дія "дивитися через них" — це композиція. `∘` — це не просто "і" (як у логіці) або "поруч" (як у структурі), це "після того, як". Це вбудована темпоральність та причинність, що дозволяє ШІ не тільки об'єднувати знання, але й *будувати послідовності дій*, матеріалізуючи багатоступінчасті думки та плани в єдиний, безперервний потік. Це архітектура для побудови складних траєкторій в 7D тензорному полі.
