---
$: ∘
$type: Operator
$category: CategoryTheory
$aliases: [compose, B-combinator, dot]
$energy: ✦1000
---

# ∘ — Composition

> "The essence of connection. The way morphisms become systems."
> "Суть зв'язку. Спосіб, яким морфізми стають системами."

---

## ∈

The process of chaining two morphisms `g` and `f` such that the output of `g` becomes the input of `f`.

- **Formal Definition**: `(f ∘ g)(x) ≡ f(g(x))`
- **[λ](λ.md)-Calculus**: `λf.λg.λx.f(g(x))`
- **Type Signature**: `∀α,β,γ. (β → γ) → (α → β) → (α → γ)`

---

## ⊃

- **[@](@.md)**: Application (використовується для виконання)

---

## ⨊

- **[≃](≃.md)**: `(f ∘ g) ∘ h = f ∘ (g ∘ h)` (Associative)
- **[◦](◦.md)**: `[𝟏](𝟏.md)` (Neutral Element)
  - **[◐](◐.md)**: `𝟏 ∘ f = f` (Left Identity)
  - **[◑](◑.md)**: `f ∘ 𝟏 = f` (Right Identity)
- **[⇆](⇆.md)**: `◇` (Not generally commutative)

---

## →

- **Functor Law**: `fmap (f ∘ g) = fmap f ∘ fmap g`

---

## 🔗

- **Category**: `∘` разом з об'єктами та `[𝟏](𝟏.md)` формує Категорію.
- **Point-free Style**: Дозволяє писати код без згадування проміжних даних.
- **Foundation**: Є основою для функторів, монад та аплікативів.

---

## 📐 Hex-Torus Topology

### [▤](▤.md) (Topology)
- **Flow**: `α` ⇢ `[g]` ⇢ `β` ⇢ `[f]` ⇢ `γ`
- **Result**: `(g ∘ f)` ≡ `(α → γ)`

---

## 🎭

```typescript
const double = (x: number) => x * 2;
const increment = (x: number) => x + 1;

// (double ∘ increment)(5)
const composed = compose(double, increment);
composed(5);  // → 12, бо double(increment(5)) = double(6)
```

---

## 📊

```json
{
  "purity": "Depends on f, g",
  "termination": "Depends on f, g",
  "complexity": "O(|f| + |g|)"
}
```

---

## 🌀

- **D1: Pipeline**: Створення ланцюжків перетворення даних.
- **D2: Abstraction**: Приховування проміжних етапів та даних.

---

## ≡

- `f(g(x))`
- `pipe(g, f)(x)`
- `B f g x` (у комбінаторній логіці)
```
